# 为光绘课堂 (LightDraw) 贡献

感谢你愿意帮助改进光绘课堂。项目欢迎代码、测试、文档、设计、翻译、教学案例和问题反馈等多种形式的贡献。

## 开始之前

1. 阅读仓库的 `README.md`，确认建议符合“教学优先、跨平台、计算与界面分离”的方向。
2. 搜索已有 Issue，避免重复报告或重复实现。
3. 修复小缺陷或完善文档可以直接提交 Pull Request；新增光学元件、修改场景格式或进行较大重构时，请先创建 Issue 讨论。
4. 不要提交密钥、账号信息、学生个人信息、未获授权的教材内容或来源不明的素材。

## 开发流程

1. Fork 仓库并从主分支创建一个内容单一、名称清晰的分支。
2. 保持改动范围聚焦，不要在同一个提交中混入无关格式化或重构。
3. 为算法和行为变化补充测试；若当前模块尚无测试项目，请在 Pull Request 中说明验证方法和后续测试建议。
4. 更新受影响的 README、场景格式说明或界面文案。
5. 在提交前执行：

   ```powershell
   dotnet restore LightDraw.slnx
   dotnet build LightDraw.slnx -c Release
   ```

6. 在 Windows、macOS 或 Linux 上进行过手动验证时，请在 Pull Request 中注明平台与操作步骤。

## 代码约定

- 遵循仓库现有的 C# 风格，启用可空引用类型并消除所有编译警告；
- 核心几何和模拟逻辑放在 `LightDraw.Core`，不得依赖 Avalonia、SkiaSharp 或平台 API；
- 绘制逻辑放在 `LightDraw.Rendering.Skia`，桌面窗口和用户流程放在 `LightDraw.Desktop`；
- 公共 API、复杂数学公式和不直观的边界处理应提供简洁注释；
- 浮点比较必须考虑容差、退化线段、平行/共线以及极端缩放场景；
- 新增持久化字段时应考虑旧文件兼容性；破坏性变化必须提升 `dataVersion` 并提供迁移方案；
- 用户可见文案优先使用清晰、准确的简体中文，避免不必要的技术术语。

## 缺陷报告

请尽量提供操作系统、架构和 .NET SDK 版本，出现问题的提交或应用版本，最小复现步骤，实际与预期结果，以及可公开的场景文件、截图或错误日志。涉及光学计算错误时，请尽量给出几何参数、理论结果或可核对的教材/论文来源。

## Pull Request 说明

Pull Request 应说明要解决的问题及教学价值、实现方式和重要取舍、自动测试与手动验证结果、UI/格式/性能/兼容性影响，以及引入的第三方内容和许可证。

## 许可证与原创性

本项目采用 PolyForm Noncommercial License 1.0.0。提交 Issue、Pull Request 或其他贡献即表示：

- 你创作了该贡献，或已获得足够权利按本项目许可证提供它；
- 你同意你的贡献按照本仓库当前许可证向公众提供；
- 你理解该许可证仅授权非商业用途，且本项目属于源码可用而非 OSI 认证开源项目；
- 你已披露所有第三方代码、数据、图片、字体或文字的来源和许可证。

请勿从许可证不兼容的项目直接复制代码。仅仅标注来源并不能自动解决许可证冲突；不确定时，请先创建 Issue 讨论。

## 行为准则

请尊重不同经验背景和观点，围绕问题本身进行讨论。禁止人身攻击、骚扰、歧视、公开他人隐私或任何破坏社区安全的行为。维护者可以编辑、隐藏或拒绝不符合这些原则的内容，并限制持续违规者参与项目。
