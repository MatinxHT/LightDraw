<Window
    x:Class="LightDraw.Desktop.Views.AboutWindow"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="{DynamicResource About.Title}"
    Width="580"
    Height="700"
    MinWidth="520"
    MinHeight="560"
    Background="#0B1220"
    Icon="/Assets/Icons/LightDraw.ico"
    WindowStartupLocation="CenterOwner">
    <Grid Margin="28" RowDefinitions="*,Auto">
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,10,18" Spacing="16">
                <StackPanel Orientation="Horizontal" Spacing="14">
                    <Image
                        Width="72"
                        Height="72"
                        Source="/Assets/Brand/LightDrawLogo.png"
                        Stretch="Uniform" />
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock
                            FontSize="20"
                            FontWeight="SemiBold"
                            Text="{DynamicResource App.Title}" />
                        <TextBlock x:Name="VersionText" Foreground="#7DE3FF" />
                    </StackPanel>
                </StackPanel>

                <TextBlock
                    Foreground="#B7C5DA"
                    Text="{DynamicResource About.Description}"
                    TextWrapping="Wrap" />

                <Border
                    Padding="16"
                    Background="#111B2E"
                    BorderBrush="#26344C"
                    BorderThickness="1"
                    CornerRadius="8">
                    <StackPanel Spacing="8">
                        <TextBlock FontWeight="SemiBold" Text="{DynamicResource About.Repository}" />
                        <TextBlock
                            Foreground="#9EB0CA"
                            Text="{DynamicResource About.RepositoryDescription}"
                            TextWrapping="Wrap" />
                        <HyperlinkButton
                            Padding="0"
                            HorizontalAlignment="Left"
                            Content="https://github.com/MatinxHT/LightDraw"
                            Foreground="#7DE3FF"
                            NavigateUri="https://github.com/MatinxHT/LightDraw" />
                    </StackPanel>
                </Border>

                <Border
                    Padding="16"
                    Background="#111B2E"
                    BorderBrush="#26344C"
                    BorderThickness="1"
                    CornerRadius="8">
                    <StackPanel Spacing="12">
                        <TextBlock FontWeight="SemiBold" Text="{DynamicResource About.Methods}" />
                        <StackPanel Spacing="4">
                            <TextBlock
                                FontWeight="SemiBold"
                                Foreground="#7DE3FF"
                                Text="{DynamicResource About.OpticsHeading}" />
                            <TextBlock
                                Foreground="#9EB0CA"
                                Text="{DynamicResource About.OpticsDescription}"
                                TextWrapping="Wrap" />
                        </StackPanel>
                        <StackPanel Spacing="4">
                            <TextBlock
                                FontWeight="SemiBold"
                                Foreground="#7DE3FF"
                                Text="{DynamicResource About.ElectroHeading}" />
                            <TextBlock
                                Foreground="#9EB0CA"
                                Text="{DynamicResource About.ElectroDescription}"
                                TextWrapping="Wrap" />
                        </StackPanel>
                        <StackPanel Spacing="4">
                            <TextBlock
                                FontWeight="SemiBold"
                                Foreground="#7DE3FF"
                                Text="{DynamicResource About.MagneticHeading}" />
                            <TextBlock
                                Foreground="#9EB0CA"
                                Text="{DynamicResource About.MagneticDescription}"
                                TextWrapping="Wrap" />
                        </StackPanel>
                        <TextBlock
                            FontSize="12"
                            Foreground="#7F93B2"
                            Text="{DynamicResource About.Disclaimer}"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </Border>

                <Border
                    Padding="16"
                    Background="#111B2E"
                    BorderBrush="#26344C"
                    BorderThickness="1"
                    CornerRadius="8">
                    <StackPanel Spacing="7">
                        <TextBlock FontWeight="SemiBold" Text="{DynamicResource About.License}" />
                        <TextBlock Foreground="#7DE3FF" Text="PolyForm Noncommercial License 1.0.0" />
                        <TextBlock
                            Foreground="#9EB0CA"
                            Text="{DynamicResource About.LicenseDescription}"
                            TextWrapping="Wrap" />
                        <TextBlock
                            FontSize="12"
                            Foreground="#7F93B2"
                            Text="Required Notice: Copyright 2026 LightDraw contributors"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </Border>

                <Border
                    Padding="16"
                    Background="#111B2E"
                    BorderBrush="#26344C"
                    BorderThickness="1"
                    CornerRadius="8">
                    <StackPanel Spacing="9">
                        <TextBlock FontWeight="SemiBold" Text="{DynamicResource About.Acknowledgements}" />
                        <TextBlock
                            Foreground="#9EB0CA"
                            LineHeight="22"
                            Text="{DynamicResource About.Institutions}"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="1" ColumnDefinitions="*,Auto">
            <TextBlock
                VerticalAlignment="Center"
                FontSize="12"
                Foreground="#687D9D"
                Text="{DynamicResource About.BuiltWith}" />
            <Button
                Grid.Column="1"
                MinWidth="88"
                HorizontalAlignment="Right"
                Click="OnCloseClicked"
                Content="{DynamicResource About.Close}"
                IsCancel="True"
                IsDefault="True" />
        </Grid>
    </Grid>
</Window>
